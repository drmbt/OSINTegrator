[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "osintegrator-api"
version = "0.1.0"
description = "FastAPI scaffold for the OSINTegrator local-first OSINT IDE."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "pydantic>=2.9,<3.0",
  "httpx>=0.28,<1.0"
]

[tool.setuptools.packages.find]
include = ["app*"]
