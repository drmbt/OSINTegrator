# Worker Stubs

This directory holds Python worker and tool adapter scaffolding for OSINTegrator.

The first iteration uses stub adapters so the UI and API can model tool runs, evidence,
and claims without requiring Sherlock, SpiderFoot, or other external tools to be
installed yet.
